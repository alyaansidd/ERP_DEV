services:
  mongodb:
    image: mongo:latest
    container_name: college-erp-mongodb
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: college-erp
    volumes:
      - mongodb_data:/data/db

volumes:
  mongodb_data:
